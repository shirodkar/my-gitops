apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: openvsx
  namespace: openvsx
spec:
  to:
    kind: Service
    name: openvsx-server
    weight: 100
  port:
    targetPort: 8080
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
